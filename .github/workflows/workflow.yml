name: workflow.yml
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test path 1
        run: |
          pwd
          ls -a
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Test path 2
        run: |
          pwd
          ls -a